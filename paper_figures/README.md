# Paper figures

This folder contains generated figures and summary CSV files used in the manuscript. They are included as reference outputs so readers can compare regenerated figures with the submitted version.

Training logs, trained models, and per-benchmark generated figures are intentionally excluded from version control.
